# Web App - Recieve POST requests, find Form Templates.
## Launching Web App can be done:
1.	 via docker command:
`
docker build --tag post_app .
docker run -p 5000:5000 -d post_app
`
Visit 'localhost:5000' to check if the setup is successful.
2.	via virtualenv:
`

`